export const RESUME_SUMMARY_JOB_NAMES = {
	GENERATE: 'generate',
	RECONCILE: 'reconcile',
} as const;

export const RESUME_SUMMARY_RECONCILIATION_SCHEDULER = 'resume-summary-reconciliation';

export const RESUME_SUMMARY_RECONCILIATION_INTERVAL_MS = 10 * 60 * 1000;

export const RESUME_SUMMARY_RECONCILIATION_BATCH_SIZE = 100;
