{
  "name": "@resume-builder/entities",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "clean": "del-cli dist",
    "prebuild": "npm run clean",
    "build": "tsc --build",
    "prewatch": "npm run clean",
    "watch": "tsc --build --watch",
    "dev": "node dist/test.js",
    "test": "nx vite:test",
    "load": "node dist/data/loader.js",
    "migrate:add-company": "node dist/data/migrate-add-company.js",
    "migrate:add-uid": "node dist/data/migrate-add-uid.js"
  },
  "private": true,
  "dependencies": {
    "@nestjs/graphql": "^13.2.3",
    "@nestjs/mongoose": "^11.0.4",
    "@typegoose/typegoose": "^13.0.0",
    "reflect-metadata": "^0.1.13",
    "zod": "^4.3.4"
  },
  "devDependencies": {
    "vite-plugin-emit-metadata": "^0.2.0"
  }
}
