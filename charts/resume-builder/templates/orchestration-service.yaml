apiVersion: v1
kind: Service
metadata:
  name: {{ include "resume-builder.orchestration.fullname" . }}
  labels:
    {{- include "resume-builder.labels" . | nindent 4 }}
    app.kubernetes.io/component: orchestration
spec:
  type: {{ .Values.orchestration.service.type }}
  ports:
    - port: {{ .Values.orchestration.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "resume-builder.orchestration.selectorLabels" . | nindent 4 }}
