apiVersion: v1
kind: Service
metadata:
  name: {{ include "resume-builder.crdt.fullname" . }}
  labels:
    {{- include "resume-builder.labels" . | nindent 4 }}
    app.kubernetes.io/component: crdt
spec:
  type: {{ .Values.crdt.service.type }}
  ports:
    - port: {{ .Values.crdt.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "resume-builder.crdt.selectorLabels" . | nindent 4 }}
